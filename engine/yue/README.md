# YueScript Framework

This folder contains the YueScript framework classes (object, timer, spring, collider, etc.).

These files are compiled to Lua and used by games via:
```yuescript
object = require 'yue.object'
timer = require 'yue.timer'
```

See `reference/phase-10-implementation-plan.md` for implementation details.
